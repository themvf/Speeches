name: Substack Public Sources Hourly

on:
  schedule:
    - cron: "17 * * * *"
  workflow_dispatch:
    inputs:
      keywords:
        description: "Optional comma-separated Substack search keywords. Leave blank to use current Intel Feed topic-rule keywords."
        required: false
        default: ""
      max_pages:
        description: "Search result pages per keyword"
        required: false
        default: "1"
      extraction_limit:
        description: "Maximum retained posts to extract"
        required: false
        default: "60"
      model:
        description: "DeepSeek model for relevance filtering"
        required: false
        default: "deepseek-v4-flash"

permissions:
  contents: read

concurrency:
  group: sec20-neon-corpus-writers
  cancel-in-progress: false

jobs:
  substack-public:
    # SEC-20: Substack also depends on paid relevance classification. Keep its
    # schedule independent so the verified Bloomberg/NewsAPI pilots can run
    # while DeepSeek is unavailable; manual recovery stays available.
    if: github.event_name != 'schedule' || vars.ENABLE_NEON_SUBSTACK_SCHEDULES == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 45
    env:
      OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
      GCS_BUCKET_NAME: ${{ secrets.GCS_BUCKET_NAME }}
      GCS_CREDENTIALS_JSON: ${{ secrets.GCS_CREDENTIALS_JSON }}
      DATABASE_URL: ${{ secrets.DATABASE_URL }}
      NEON_BACKFILL_VERIFIED: "true"
      DEEPSEEK_API: ${{ secrets.DEEPSEEK_API }}
      ENRICH_MODEL: deepseek-v4-pro
      RESIDENTIAL_PROXY_URL: ${{ secrets.RESIDENTIAL_PROXY_URL }}
      SUBSTACK_PROXY_URL: ${{ secrets.SUBSTACK_PROXY_URL || secrets.RESIDENTIAL_PROXY_URL || secrets.APIFY_PROXY_URL }}
      KEYWORDS: ${{ github.event.inputs.keywords || '' }}
      MAX_PAGES: ${{ github.event.inputs.max_pages || '1' }}
      EXTRACTION_LIMIT: ${{ github.event.inputs.extraction_limit || '60' }}
      MODEL: ${{ github.event.inputs.model || 'deepseek-v4-flash' }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Extract and Filter Substack Public Sources
        run: |
          set -euo pipefail
          python run_connector_extraction_pipeline.py \
            --connector substack_public_article \
            --persistence-mode neon_authoritative \
            --keywords "${KEYWORDS}" \
            --relevance-provider deepseek \
            --relevance-model "${MODEL}" \
            --selection new_or_updated \
            --limit "${EXTRACTION_LIMIT}" \
            --max-pages "${MAX_PAGES}" \
            --include-rss true \
            --require-remote-persistence \
            --summary-path substack_public_extraction_summary.json

      - name: Check for Substack Changes
        id: extraction_summary
        run: |
          python - <<'PY'
          import json
          import os
          from pathlib import Path

          path = Path("substack_public_extraction_summary.json")
          payload = json.loads(path.read_text(encoding="utf-8")) if path.exists() else {}
          changed_count = int(payload.get("saved_new", 0) or 0) + int(payload.get("saved_updates", 0) or 0)
          with open(os.environ["GITHUB_OUTPUT"], "a", encoding="utf-8") as output:
              output.write(f"changed_count={changed_count}\n")
          print(f"Substack new or updated documents: {changed_count}")
          PY

      - name: Run Substack Enrichment
        if: steps.extraction_summary.outputs.changed_count != '0'
        run: |
          set -euo pipefail
          python run_financial_news_pipeline.py enrich \
            --require-remote-persistence \
            --persistence-mode neon_authoritative \
            --source-kind substack_public_article \
            --mode all \
            --doc-ids-from-summary substack_public_extraction_summary.json \
            --provider deepseek \
            --model "${ENRICH_MODEL}" \
            --summary-path substack_public_enrich_summary.json

      - name: Run Substack Bounded Enrichment Catch-up
        if: always()
        run: |
          set -euo pipefail
          python run_financial_news_pipeline.py enrich \
            --require-remote-persistence \
            --persistence-mode neon_authoritative \
            --source-kind substack_public_article \
            --mode only_missing_or_failed \
            --limit 10 \
            --provider deepseek \
            --model "${ENRICH_MODEL}" \
            --summary-path substack_public_enrich_catchup_summary.json

      - name: Upload Substack Summaries
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: substack-public-hourly-${{ github.run_id }}
          path: |
            substack_public_extraction_summary.json
            substack_public_enrich_summary.json
            substack_public_enrich_catchup_summary.json
          if-no-files-found: ignore
